# Initialize Git
git init

# Add all your files
git add .

# Make your first commit
git commit -m "feat: initial release of Entropic Synthesis v3"

# Add GitHub as remote (replace YOUR_USERNAME with your actual GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/entropic-synthesis.git

# Push to GitHub (main branch)
git branch -M main
git push -u origin main
